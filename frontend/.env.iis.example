VITE_API_URL=http://localhost:8081/api

# Google OAuth Web Client ID (Google Identity Services). Must match backend GoogleAuth__ClientId.
# Leave blank to hide the "Continue with Google" button until you create one — see iis/README.md.
VITE_GOOGLE_CLIENT_ID=
